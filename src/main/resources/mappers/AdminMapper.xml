<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aris.javaweb_vtd.mapper.AdminMapper">
    <insert id="insertAdmin"
        parameterType="com.aris.javaweb_vtd.dto.request.AdminRequestDTO"
        useGeneratedKeys="true"
        keyProperty="id">
      INSERT INTO admin 
        (username
        ,password
        ,created_at
        ,updated_at)
      VALUES 
        (#{username}
        ,#{password}
        ,#{createdAt}
        ,#{updatedAt})
    </insert>
    
    <update id="updatePassword" parameterType="com.aris.javaweb_vtd.dto.request.AdminRequestDTO">
      UPDATE admin
      SET 
        password = #{password}, 
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <select id="findByUsername" resultType="com.aris.javaweb_vtd.dto.request.AdminRequestDTO">
      SELECT 
        a.id, 
        a.username, 
        a.password
      FROM admin a
      WHERE a.username = #{username}
      LIMIT 1
    </select>

</mapper>